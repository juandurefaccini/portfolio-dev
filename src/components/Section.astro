---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class={`py-8 ${className}`}>
  <div class="container mx-auto max-w-5xl px-4">
    <slot />
  </div>
</section>
